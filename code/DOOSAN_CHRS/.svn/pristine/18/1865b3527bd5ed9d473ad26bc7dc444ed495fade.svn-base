function CheckForm()
{	
	if(validateDate1(document.save.cell_phone.value))
		{}
	else{
		alert ("\n 日期格式不正确!");
		document.save.cell_phone.focus();
		return false;
	}
	
	if(validateDate2(document.save.cell_phone.value))
		{}
	else{
		alert ("\n 日期格式不正确!");
		document.save.cell_phone.focus();
		return false;
	}
	
	if(IsNumber(document.save.tel.value))
		{}
	else{
		alert ("\n 电话号码只能是数字!");
		document.save.tel.focus();
		return false;
	}
	
	if(IsEmail(document.save.email.value))
		{}
	else{
		alert ("\n 邮箱格式不正确!");
		document.save.email.focus();
		return false;
	}
	
	if(NumCount_4(document.save.num4.value))
		{}
	else{
		alert ("\n 只能输入4个数字!");
		document.save.num4.focus();
		return false;
	}
}